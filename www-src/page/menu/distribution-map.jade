ion-view(view-title="Distributions")
	ion-nav-buttons(side="secondary")
		button.button.button-clear.icon.ion-more.button-stable(ng-click="showOptions($event)")

	ion-content(scroll="false")
		#distribution-map(style="height: 100%")

	script#distribution-map-options(type="text/ng-template")
		ion-popover-view
			.list
				.item.item-divider
					p Filter
				ion-toggle(ng-model="view.others") Include Others
				.item.item-input
					input(type="text", ng-model="view.name", placeholder="Fish Name Filter")
				.item.item-divider
					p Map View
				ion-radio(ng-repeat="(text, value) in view.gmap.types", ng-model="view.gmap.type", ng-value="value") {{ text }}
