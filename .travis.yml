osx_image: xcode7
language: objective-c
jdk:
  - oraclejdk8
dart: stable
node_js: 0.12

cache:
  bundler: true
  directories:
    - node_modules
    - plugins
    - $HOME/.pub-cache
    - $HOME/.ivy2
    - $HOME/.gradle

install:
  - brew tap dart-lang/dart && brew install dart
  - brew install android
  - export ANDROID_HOME=$(brew --prefix android) && echo $ANDROID_HOME
  - brew install sbt
  - npm install
  - gem install compass
  - for dir in $(pwd)/node_modules/*/bin; do export PATH=$PATH:$dir; done; echo $PATH

before_script:
  - . travis-scripts/build_mode
  - echo "BUILD_MODE=$BUILD_MODE"

script:
  - mkdir -pv dart/build/web || travis-scripts/dart.sh
  - travis-scripts/cordova-prepare.sh
  - travis-scripts/android-build.sh
  - travis-scripts/ios-build.sh
